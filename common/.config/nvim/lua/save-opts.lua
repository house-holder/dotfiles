--------------------------------------------------------------------------------
-- save-opts allows the creation of custom config persistence
--------------------------------------------------------------------------------

-- 1. check for the data file
-- 2. if it exists, load items within as vim.g.* options
-- 3. define the generic shape of new option setter commands
-- 4. make new user commands that follow the pattern
