[[ $- != *i* ]] && return

export PS1='\u@\h:\w\$ '
export PATH="$HOME/bin:$HOME/.local/bin:/usr/local/bin:/usr/bin:/bin"

# basic completion
if [ -f /etc/bash_completion ]; then
  . /etc/bash_completion
fi
